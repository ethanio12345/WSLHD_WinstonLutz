[pytest]
markers =
    integration: marks tests as integration tests (require Docker daemon; run with '-m integration')
    e2e: marks tests as end-to-end browser tests (require Playwright + running Streamlit server)
addopts = -m "not integration and not e2e"
testpaths = tests
